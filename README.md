# Billing System

Python-based billing system for processing CDR (Call Detail Records) and generating financial transactions.

## Setup

1. Create virtual environment:
```bash
python3 -m venv venv
source venv/bin/activate